Config = Config or {}




Config.CustomCashRegisters = {
    -- ["jobname"] = { -- Player job role restriction
    -- 	{ coords = vec3(0, 0, 0, 0), prop = true, }, -- vec3 to place the till and the way it faces
    -- 	{ coords = vec3(0, 0, 0, 0), prop = true, }, -- "prop = true" spawns a prop at the coords
    -- },

    ["mechanic"] = { -- Player job role restriction
     	{ coords = vec3(124.36933, -3028.000, 6.800), prop = true, }, -- vec3 to place the till and the way it faces
    },
}

